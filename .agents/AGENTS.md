# Rules for Antigravity Coding Assistant

- **Git Branch Management**: Do NOT automatically merge git branches (e.g. `git merge`) from feature branches to the `main` branch. All branch merges must be explicitly approved by the user or requested before execution. You should only push your commits to the active feature branch (`feature/refactor-auto-farm` or similar).
