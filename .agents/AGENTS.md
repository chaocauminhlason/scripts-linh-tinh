# Rules for Antigravity Coding Assistant

- **Git Branch Management**: Do NOT automatically merge git branches (e.g. `git merge`) from feature branches to the `main` branch unless explicitly approved or requested by the user. When requested, perform the merge safely. You should normally push your commits to the active feature branch.
